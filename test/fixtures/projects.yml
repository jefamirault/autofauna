# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  owner: one
  name: Test Project
  active: true

two:
  owner: two
  name: Second Project
  active: true

# Default project for the non-onboarded user (every real user gets one on create)
project_fresh:
  owner: fresh
  name: Fresh Project
  active: true

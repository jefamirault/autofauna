<div class="max-w-4xl mx-auto px-4 py-8">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-gray-900">Edit Water Test</h1>
    <p class="text-gray-600 mt-1">Update test from <%= @water_test.tested_at.strftime("%B %d, %Y at %I:%M %p") %></p>
  </div>

  <%= render 'form' %>
</div>
<% content_for :title, "TDS Readings" %>

<h1>Total Dissolved Solids (TDS) Readings</h1>

<%= link_to "+ New TDS Reading", new_tds_reading_path %>

<%= render partial: 'tds_readings', locals: { tds_readings: @tds_readings } %>

<% if @tds_readings&.any? %>
<%= link_to "+ New TDS Reading", new_tds_reading_path %>
<% end %>

<div class="settings-card">
  <% content_for :title do %>
    New Plant
  <% end %>
  <h2><%= t 'plants.index.new_plant' %></h2>
  <%= render "form", plant: @plant %>

  <div class="card-footer-actions">
    <%= link_to "❌ #{t('actions.cancel')}", plants_path, class: 'link' %>
  </div>
</div>
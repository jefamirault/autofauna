<% content_for :title, "Tanks" %>

<%= link_to "➕ New Tank", new_tank_path, class: 'link' %>

<div class="resource-cards">
  <% @tanks.each do |tank| %>
    <%= render tank %>
  <% end %>
</div>

<% if @tanks.any? %>
  <%= link_to "➕ New Tank", new_tank_path, class: 'link' %>
<% end %>

Your plants need watering!

Hi <%= @user.email %>,

The following plants are due for watering:

<% @plants.each do |plant| %>
- <%= plant.label %><% if plant.date_last_watering %> — last watered <%= plant.date_last_watering.strftime("%b %d") %><% end %>
  <%= plant_url(plant) %>
<% end %>

Open Autofauna: <%= root_url %>

plant_notifications:
  cron: '* * * * *'  # Every minute — job checks per-user frequency preferences
  class: 'PlantNotificationJob'
  active_job: true
  description: 'Send watering reminder notifications to users'

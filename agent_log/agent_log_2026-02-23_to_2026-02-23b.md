# Agent Log

Current session log. Previous logs archived in `agent_log/`.

---

## 2026-02-23: Test Suite Overhaul

### Fixed Existing Tests (54 tests, 45 failures/8 errors → 0)

**Root causes:**
- No test authentication — added `sign_in(user)` helper to `test_helper.rb`
- Fixtures used hardcoded IDs instead of fixture references — rewrote all fixture files
- Stale attribute references (`Watering#date` → `Watering#watered_at`, `Tank#zone_id` removed)
- Sessions controller test used wrong route helpers (`sessions_new_url` → `new_session_url`) and HTTP methods

**Files changed:**
- `test/test_helper.rb` — Added `sign_in` helper for integration tests
- `test/fixtures/*.yml` — All rewritten with proper associations, BCrypt passwords, unique UIDs
- `test/controllers/sessions_controller_test.rb` — Fixed route helpers, HTTP methods, added meaningful assertions
- `test/controllers/plants_controller_test.rb` — Added auth, fixed params
- `test/controllers/waterings_controller_test.rb` — Added auth, fixed `date` → `watered_at`, added `plant_id` to new
- `test/controllers/tanks_controller_test.rb` — Added auth, fixed params (added `location_id`)
- `test/controllers/locations_controller_test.rb` — Added auth, handled FK in destroy
- `test/controllers/zones_controller_test.rb` — Added auth, destroy uses `:two` to avoid FK
- `test/controllers/sensors_controller_test.rb` — Added auth, added `sensor_type_id`
- `test/controllers/sensor_types_controller_test.rb` — Added auth, destroy uses `:two`
- `test/models/plant_test.rb` — Fixed `date` → `watered_at`, explicit `update_watering_dates` call

### App Bugs Fixed

- `app/models/plant.rb` — 4 references to `w.date` → `w.watered_at` (lines 57, 65, 74, 75)
- `app/models/plant.rb` — `calculate_watering_frequency` used DateTime subtraction (returns seconds) instead of Date subtraction (returns days) — added `.to_date`
- `app/helpers/plants_helper.rb` — `last_watering_time_short_text` crashed on nil `date_last_watering` — added nil guard
- `app/controllers/application_controller.rb` — `auto_select_project(nil)` crashed — added nil guard

### New Test Coverage (+116 tests)

Spawned 5-agent team to implement in parallel:

1. **Authorization & multi-tenancy** (28 tests) — `test/controllers/authorization_test.rb`
   - Cross-project isolation for plants, waterings, tanks, locations, zones, sensors, sensor_types
   - Verifies show/edit/update/destroy all redirect for wrong-project user
   - Added project-two fixtures (`plant_p2`, `zone_p2`, `location_p2`, etc.)

2. **Watering schedule model** (27 tests) — `test/models/plant_test.rb`
   - `update_watering_dates`, `watering_urgency`, `time_until_watering`, `calculate_watering_frequency`, `watering_frequency_text`
   - Edge cases: no waterings, no schedule, single/multiple waterings, overdue/today/normal/none urgency

3. **Notification logic** (20 tests) — `test/models/user_notification_test.rb`
   - `should_send_notification?` for daily/hourly/N-days frequencies
   - Both email and push channels, disabled states, time-of-day checks

4. **Guest account lifecycle** (22 tests) — `test/models/guest_account_test.rb`
   - Guest creation, `convert_from_guest!`, `convert_from_guest_google!`, `merge_guest!`, `plants_needing_water`
   - Merge transfers plants/zones/locations/sensors, reassigns UIDs, handles advanced_mode

5. **Shared plants & API** (13 tests) — `test/controllers/shared_plants_controller_test.rb` + `test/controllers/transmit_controller_test.rb`
   - Share token access/revocation/invalid tokens
   - Sensor API: valid requests, bad keys, missing params

### CLAUDE.md Updates

- Added Testing subsections: Test Authentication, Fixture Conventions, Writing Controller Tests
- Added Multi-Tenancy Test Fixtures table
- Added Test Coverage Summary table
- Fixed Sensor required associations (added `zone`)
